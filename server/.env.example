
# =============================================================================
# Translation Server (New Architecture) Configuration
# =============================================================================

# Provider type: mock, voicelive, live_interpreter
PROVIDER_TYPE=mock

# VoiceLive provider settings (if using voicelive)
VOICELIVE_ENDPOINT=wss://your-endpoint.com/v1/realtime
VOICELIVE_API_KEY=your-api-key

# Auto-commit thresholds
BATCH_MAX_MS=200
BATCH_MAX_BYTES=65536
BATCH_IDLE_TIMEOUT_MS=500

# Queue configuration
INGRESS_QUEUE_MAX=1000
EGRESS_QUEUE_MAX=1000
OVERFLOW_POLICY=DROP_OLDEST
