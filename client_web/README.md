# ACS Emulator Web Client

This subproject provides a Docker-only web UI for the ACS emulator.

## Build

- `make docker-build`

## Run

- `make docker-run`

## Stop

- `make docker-stop`

Open the UI at `http://localhost:8000/`.
